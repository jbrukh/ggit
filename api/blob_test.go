package api

import (
	"testing"
)

func Test_readSimpleBlobs(t *testing.T) {
	// 	dir, err := test.Repo("../test/cases/linear_history.sh")
	// 	assertNoErr(t, err)

	// 	repo := Open(dir)

	// 	ObjectFromRef(repo, )
}
